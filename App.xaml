<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="GratingPlayer.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequestedTheme="Light">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <Color x:Key="Primary">#B8893D</Color>
            <Color x:Key="PrimaryDeep">#9A6B20</Color>
            <Color x:Key="PrimarySoft">#C99A4A</Color>
            <Color x:Key="PrimaryHover">#C49642</Color>
            <Color x:Key="PrimaryPressed">#A67A2E</Color>
            <Color x:Key="CheckAccent">#005A5E</Color>
            <Color x:Key="LinkBlue">#2563EB</Color>
            <Color x:Key="Danger">#C45C5C</Color>
            <Color x:Key="DangerDeep">#A84848</Color>
            <Color x:Key="PageBg">#F3F4F6</Color>
            <Color x:Key="Surface">#FFFFFF</Color>
            <Color x:Key="SurfaceLine">#D1D5DB</Color>
            <Color x:Key="Ink">#1F2937</Color>
            <Color x:Key="InkMuted">#4B5563</Color>
            <Color x:Key="InkHint">#4B5563</Color>
            <Color x:Key="InkDisabled">#9CA3AF</Color>

            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary}" />
            <SolidColorBrush x:Key="PrimaryDeepBrush" Color="{StaticResource PrimaryDeep}" />
            <SolidColorBrush x:Key="PrimarySoftBrush" Color="{StaticResource PrimarySoft}" />
            <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource PrimaryHover}" />
            <SolidColorBrush x:Key="PrimaryPressedBrush" Color="{StaticResource PrimaryPressed}" />
            <SolidColorBrush x:Key="CheckAccentBrush" Color="{StaticResource CheckAccent}" />
            <SolidColorBrush x:Key="LinkBlueBrush" Color="{StaticResource LinkBlue}" />
            <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource Danger}" />
            <SolidColorBrush x:Key="DangerDeepBrush" Color="{StaticResource DangerDeep}" />
            <SolidColorBrush x:Key="PageBgBrush" Color="{StaticResource PageBg}" />
            <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource Surface}" />
            <SolidColorBrush x:Key="SurfaceLineBrush" Color="{StaticResource SurfaceLine}" />
            <SolidColorBrush x:Key="InkBrush" Color="{StaticResource Ink}" />
            <SolidColorBrush x:Key="InkMutedBrush" Color="{StaticResource InkMuted}" />
            <SolidColorBrush x:Key="InkHintBrush" Color="{StaticResource InkHint}" />
            <SolidColorBrush x:Key="InkDisabledBrush" Color="{StaticResource InkDisabled}" />

            <Color x:Key="SystemAccentColor">#005A5E</Color>
            <Color x:Key="SystemAccentColorLight1">#0E7A7F</Color>
            <Color x:Key="SystemAccentColorDark1">#004448</Color>
            <SolidColorBrush x:Key="SystemAccentColorBrush" Color="{StaticResource SystemAccentColor}" />
            <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="{StaticResource SystemAccentColor}" />

            <!-- 全页统一字号/字色 -->
            <x:Double x:Key="AppFontSize">14</x:Double>

            <Style x:Key="FieldLabel" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource InkMutedBrush}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSize}" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style x:Key="SectionLabel" TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style x:Key="UnitLabel" TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}" />

            <Style x:Key="Action" TargetType="Button">
                <Setter Property="FontSize" Value="{StaticResource AppFontSize}" />
                <Setter Property="Padding" Value="10,0" />
                <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
                <Setter Property="Background" Value="#E8ECF2" />
                <Setter Property="BorderBrush" Value="#D1D5DB" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
            </Style>

            <!-- 自定义模板：悬停用邻近金色，避免变白 -->
            <Style x:Key="PrimaryAction" TargetType="Button">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryDeepBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    FontSize="{TemplateBinding FontSize}"
                                    FontWeight="{TemplateBinding FontWeight}"
                                    Foreground="{TemplateBinding Foreground}" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PrimaryHoverBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PrimaryPressedBrush}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="#D6C29A" />
                                                </ObjectAnimationUsingKeyFrames>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="0.7" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="DangerAction" TargetType="Button" BasedOn="{StaticResource Action}">
                <Setter Property="Background" Value="#FCECEC" />
                <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                <Setter Property="BorderBrush" Value="#F0C8C8" />
                <Setter Property="Padding" Value="10,4" />
            </Style>

            <Style x:Key="SchemeLink" TargetType="HyperlinkButton">
                <Setter Property="Foreground" Value="{StaticResource LinkBlueBrush}" />
                <Setter Property="FontSize" Value="{StaticResource AppFontSize}" />
                <Setter Property="Padding" Value="0,2" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
